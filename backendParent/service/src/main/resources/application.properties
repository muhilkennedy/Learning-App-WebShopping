spring.application.name = MINIPROJECT
project.deployment.mode = prod

# Actuator info
info.app.name = MINIPROJECT
info.app.description = This project uses Angular9, Spring boot and mySQL
info.app.version = 1.0.0

# logging level
logging.level.org.springframework = ERROR
logging.level.com.backend = DEBUG

# Server configuration
server.port=8080

# Datasource configuration
spring.datasource.url = jdbc:mysql://localhost:3306/clouddatabase
spring.datasource.username = root
spring.datasource.password = root@123
spring.datasource.driver-class-name = com.mysql.jdbc.Driver

# JPA
spring.jpa.database-platform = org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

#JWT implementation
spring.security.jwt.secret = private-Learning_app@2020

# Google OAuth Details
spring.social.gmail.enable-mailing = false
spring.social.gmail.id = do.not.reply.application.ordering@gmail.com
spring.social.gmail.password = <gmail-password>
spring.social.google.app-id = <app-id>
spring.social.google.app-secret = <app-secret>
spring.social.google.redirectUri = http://localhost:8080/social/google
spring.social.google.scope = profile email
